#include <bits/stdc++.h>
#define endl '\n'
#define opt() ios_base::sync_with_stdio(0); cout.tie(0); cin.tie(0);
using namespace std;
int main()
{
    opt();
    int a,b,x; cin >> a >> b >> x;
    if(a<=x && a+b>x)
        cout << "YES";
    else
        cout << "NO";
    return 0;
}
