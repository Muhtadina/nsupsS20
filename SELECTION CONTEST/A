#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N,D, count=0; cin >> N >> D;
    string S; cin >> S;
    for(int i=0; i<N; i++)
    {
        if(S[i]=='.')
            count++;
    }
    cout<<count+D;
    return 0;
}
